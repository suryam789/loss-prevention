## Build Commands:

### Build sensor simulate Docker image

- to build run:

    ```bash
    make build
    ```

- to run sensor simulate:

    ```bash
    make run
    ```

- to shutdown Docker containers

    ```bash
    make down
    ```